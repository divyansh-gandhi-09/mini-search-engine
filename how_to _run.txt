make a folder "data" all files go there after indexed index.json will be generated 
first for clean build - mingw32-make clean
then build - mingw32-make
use generate_test_docs with - python generate_test_docs.py --n 2000 --words 1000 //uses natural language toolkit to gerate unique word and documents
upload documents of types 📄 Supported formats: 18
   • .pdf    - PDF documents
   • .png    - PNG images
   • .jpg    - JPEG images
   • .jpeg   - JPEG images
   • .tiff   - TIFF images
   • .bmp    - BMP images
   • .gif    - GIF images
   • .txt    - Plain text files
   • .md     - Markdown files
   • .csv    - CSV files
   • .json   - JSON files
   • .log    - Log files
   • .xml    - XML files
   • .html   - HTML files
   • .htm    - HTML files
   • .docx   - Word documents (if python-docx installed)
   • .xlsx   - Excel files (if openpyxl installed)
   • .xls    - Excel files (if pandas installed)
make sure py extractor.py
if need to delete all test document - python generate_test_docs.py --clean
index.json will detect any changes in the data folder then reindex automatically so no need to delete it 
to run the server - ./server.exe
to run the console_main ,replace the server.cpp with console_main.cpp-
g++ -I"header files" implementation/*.cpp -o main_console.exe and ./main_console.exe  console_main
